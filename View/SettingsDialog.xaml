<?xml version="1.0" encoding="utf-8"?>
<ContentDialog x:Class="AppGroup.View.SettingsDialog"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               CornerRadius="8"
               MinWidth="500"
              >

    <ContentDialog.Resources>
        <Thickness x:Key="ContentDialogPadding">0,0,0,0</Thickness>

        <!-- Custom styles for better consistency -->
        <Style x:Key="SettingsCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Padding" Value="20,16"/>
            <Setter Property="Margin" Value="0,0,0,2"/>
        </Style>

        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}"/>
            <Setter Property="Margin" Value="0,20,0,12"/>
        </Style>
    </ContentDialog.Resources>

    <StackPanel Spacing="10" VerticalAlignment="Center" Background="{ThemeResource LayerOnMicaBaseAltFillColorSecondaryBrush}" CornerRadius="5" HorizontalAlignment="Stretch">
        <Grid Padding="10,0,5,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

        <TextBlock x:Name="EditTitle" FontSize="20" Margin="10" VerticalAlignment="Center" FontWeight="SemiBold">
            설정
        </TextBlock>
            <Button Click="CloseDialog" Grid.Column="1" Height="35" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0">
                <FontIcon Glyph="&#xe711;" FontSize="15"/>
            </Button>
        </Grid>

        <ScrollViewer MaxHeight="450" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="0" Margin="20,0,20,20">

                <!-- Application Section -->

                <Border Style="{StaticResource SettingsCardStyle}">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" MinWidth="200"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,20,0">
                                <TextBlock Text="시작 시 실행"
                                         FontWeight="SemiBold"
                                         FontSize="14"
                                         TextWrapping="Wrap"/>
                                <TextBlock Text="Windows 시작 시 자동으로 앱 실행"
                                         FontSize="12"
                                         Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                         Margin="0,2,0,0"
                                         TextWrapping="Wrap"/>
                            </StackPanel>

                            <ToggleSwitch x:Name="StartupToggle"
                                         Grid.Column="1"
                                         IsOn="{Binding RunAtStartup, Mode=TwoWay}"
                                         OnContent=""
                                         OffContent=""
                                         VerticalAlignment="Center"
                                        />
                        </Grid>

                        <!-- 시작 프로그램 차단 상태 메시지 -->
                        <TextBlock x:Name="StartupStatusText"
                                   Text="{Binding StartupStatusMessage}"
                                   FontSize="12"
                                   Foreground="{ThemeResource SystemFillColorCautionBrush}"
                                   Margin="0,8,0,0"
                                   TextWrapping="Wrap"
                                   Visibility="{Binding StartupStatusVisibility}"/>
                    </StackPanel>
                </Border>

                <!-- System Tray Section -->

                <Border Style="{StaticResource SettingsCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" MinWidth="200"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,20,0">
                            <TextBlock Text="시스템 트레이 아이콘 표시"
                                     FontWeight="SemiBold"
                                     FontSize="14"
                                     TextWrapping="Wrap"/>
                            <TextBlock Text="시스템 트레이에 앱 아이콘 표시"
                                     FontSize="12"
                                     Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                     Margin="0,2,0,0"
                                     TextWrapping="Wrap"/>
                        </StackPanel>

                        <ToggleSwitch x:Name="SystemTrayToggle"
                                     Grid.Column="1"
                                     IsOn="{Binding ShowSystemTrayIcon, Mode=TwoWay}"
                                     OnContent=""
                                     OffContent=""
                                     VerticalAlignment="Center"
                                    />
                    </Grid>
                </Border>

                <Border Style="{StaticResource SettingsCardStyle}" Visibility="Collapsed">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" MinWidth="200"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,20,0">
                            <TextBlock Text="회색조 아이콘"
                                     FontWeight="SemiBold"
                                     FontSize="14"
                                     TextWrapping="Wrap"/>
                            <TextBlock Text="그룹 표시 중 아이콘을 회색조로 표시"
                                     FontSize="12"
                                     Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                     Margin="0,2,0,0"
                                     TextWrapping="Wrap"/>
                        </StackPanel>

                        <ToggleSwitch x:Name="GrayscaleIconToggle"
                                     Grid.Column="1"
                                     IsOn="{Binding UseGrayscaleIcon, Mode=TwoWay}"
                                     VerticalAlignment="Center"
                                     OnContent=""
                                     OffContent=""
                                     Toggled="GrayScaleToggle_Toggled"
                                    />
                    </Grid>
                </Border>

                <!-- About Section -->
                <TextBlock Text="정보" Style="{StaticResource SectionHeaderStyle}"/>

                <Border Style="{StaticResource SettingsCardStyle}">
                    <StackPanel>
                        <TextBlock Text="AppGroup"
                                 FontWeight="SemiBold"
                                 FontSize="14"/>
                        <TextBlock x:Name="VersionText"
                                 Text="{Binding VersionText}"
                                 FontSize="12"
                                 Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                 Margin="0,4,0,0"/>

                        <HyperlinkButton NavigateUri="https://github.com/iandiv/AppGroup"      Padding="0"
      Margin="0,12,0,4">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="AppGroup" FontWeight="SemiBold" FontSize="13" Margin="0,0,12,0" VerticalAlignment="Center"/>
                                <TextBlock Text="MIT License" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </HyperlinkButton>
                        <TextBlock Text="이 앱은 오픈소스 프로젝트 AppGroup를 기반으로 하여 UI 개선 및 기능 추가 등 일부 커스터마이징을 적용한 버전입니다." FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                        <TextBlock Text="원본 제작자와 무관하며 위 링크를 통해 원본 소스를 확인할 수 있습니다." FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>

                    </StackPanel>
                </Border>

                <!-- Open Source Licenses -->
                <TextBlock Text="Open Source Licenses" Style="{StaticResource SectionHeaderStyle}"/>

                <Border Style="{StaticResource SettingsCardStyle}">
                    <StackPanel Spacing="8">
                        <!-- CommunityToolkit.Mvvm -->
                        <HyperlinkButton NavigateUri="https://github.com/CommunityToolkit/dotnet"
                                       Padding="0"
                                       Margin="0,0,0,4">
                            <StackPanel>
                                <TextBlock Text="CommunityToolkit.Mvvm" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Text="MIT License" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            </StackPanel>
                        </HyperlinkButton>

                        <!-- Interop.IWshRuntimeLibrary -->
                        <HyperlinkButton NavigateUri="https://www.nuget.org/packages/Interop.IWshRuntimeLibrary"
                                       Padding="0"
                                       Margin="0,0,0,4">
                            <StackPanel>
                                <TextBlock Text="Interop.IWshRuntimeLibrary" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Text="Windows Script Host Object Model" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            </StackPanel>
                        </HyperlinkButton>

                        <!-- Microsoft.WindowsAppSDK -->
                        <HyperlinkButton NavigateUri="https://github.com/microsoft/WindowsAppSDK"
                                       Padding="0"
                                       Margin="0,0,0,4">
                            <StackPanel>
                                <TextBlock Text="Microsoft.WindowsAppSDK" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Text="MIT License" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            </StackPanel>
                        </HyperlinkButton>

                        <!-- System.Drawing.Common -->
                        <HyperlinkButton NavigateUri="https://github.com/dotnet/runtime"
                                       Padding="0"
                                       Margin="0,0,0,4">
                            <StackPanel>
                                <TextBlock Text="System.Drawing.Common" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Text="MIT License" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            </StackPanel>
                        </HyperlinkButton>

                        <!-- WinUIEx -->
                        <HyperlinkButton NavigateUri="https://github.com/dotMorten/WinUIEx"
                                       Padding="0"
                                       Margin="0,0,0,4">
                            <StackPanel>
                                <TextBlock Text="WinUIEx" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Text="MIT License" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            </StackPanel>
                        </HyperlinkButton>
                    </StackPanel>
                </Border>

               
            </StackPanel>
        </ScrollViewer>
    </StackPanel>
</ContentDialog>
