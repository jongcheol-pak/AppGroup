<Window
    xmlns:winuiex="using:WinUIEx"
    x:Class="AppGroup.View.FolderContentsPopupWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppGroup.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Folder Contents">
    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 헤더 영역 -->
        <StackPanel Grid.Row="0">
            <Grid x:Name="Header" Margin="10,5,10,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="HeaderText" x:Uid="FolderContentsHeader" VerticalAlignment="Center" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
            </Grid>
            <Border Height="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}" Margin="10,0,10,5"/>
        </StackPanel>

        <!-- 파일/폴더 목록 영역 -->
        <ScrollView Grid.Row="1" Padding="0,0,0,10"  VerticalScrollMode="Auto" x:Name="ScrollView" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" HorizontalScrollMode="Disabled"
                    VerticalScrollBarVisibility="Auto" Margin="5,5,5,10">
            <StackPanel x:Name="ContentsPanel" HorizontalAlignment="Stretch">
                <!-- 파일 섹션 -->
                <TextBlock x:Name="FileSectionHeader" x:Uid="FileSectionHeader" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="10,5,10,5" Visibility="Collapsed"/>
                <ItemsControl x:Name="FileItemsControl" HorizontalAlignment="Stretch">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel HorizontalAlignment="Stretch"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
                
                <!-- 폴더 섹션 -->
                <TextBlock x:Name="FolderSectionHeader" x:Uid="FolderSectionHeader" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="10,10,10,5" Visibility="Collapsed"/>
                <ItemsControl x:Name="FolderItemsControl" HorizontalAlignment="Stretch">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel HorizontalAlignment="Stretch"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
                
                <!-- 빈 상태 -->
                <TextBlock x:Name="EmptyText" x:Uid="EmptyContents" HorizontalAlignment="Center" Margin="20" 
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" Visibility="Collapsed"/>
            </StackPanel>
        </ScrollView>
    </Grid>
</Window>
